version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Update apt-get
          command: sudo apt-get update;
      - run:
          name: Install yamllint
          command: sudo apt-get install yamllint
